[package]
name = "consumer-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
ironlog_consumer = { path = "../consumer", package = "consumer" }
ironlog_core = { path = "../core", package = "core" }
compio = { version = "0.19.0", features = ["net", "macros", "dispatcher"] }
