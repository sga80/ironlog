[package]
name = "producer-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
ironlog_producer = { path = "../producer", package = "producer" }
ironlog_core = { path = "../core", package = "core" }
compio = { version = "0.19.0", features = ["net", "macros", "dispatcher"] }