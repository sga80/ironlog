# IronLog V4 — Design Document

V4 targets producer batching — reducing the number of kernel operations per message on the write and read path. Details
will
be documented as the implementation progresses.